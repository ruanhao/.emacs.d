# -*- mode: snippet -*-
# name: logging_config
# key: lc
# --
logging.basicConfig(
    level=logging.DEBUG,
    format='[%(asctime)s] %(levelname)-8s - %(message)-100s <%(pathname)s:%(lineno)d>',
    datefmt="%Y/%m/%d %H:%M:%S"
)
$0