# -*- mode: snippet -*-
# name: yas_io_poll
# key: yas_io_poll
# --
int Poll(struct pollfd *fdarray, unsigned long nfds, int timeout)
{
    int n;
    if ((n = poll(fdarray, nfds, timeout)) < 0)
        err_sys("poll() error");
    return(n);
}
