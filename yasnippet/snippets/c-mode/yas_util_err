# -*- mode: snippet -*-
# name: yas_util_err
# key: yas_util_err
# --
void err_quit(const char *fmt, ...) {
    char *str;
    va_list ap;
    va_start(ap, fmt);
    vasprintf(&str, fmt, ap);
    va_end(ap);
    perror(str);
    free(str);
    exit(1);
}
